diff --git a/index-4pvh4sbk.js b/index-4pvh4sbk.js
index b2e399c1e0282772da2f2d7786c985580952cb2f..aa893cd434a516be97bc82f85e9f9e0d4265ab66 100644
--- a/index-4pvh4sbk.js
+++ b/index-4pvh4sbk.js
@@ -17959,6 +17959,16 @@ class CodeRenderable extends TextBufferRenderable {
     const isInitialContent = this._streaming && !this._hadInitialContent;
     const shouldDrawUnstyledNow = this._streaming ? isInitialContent && this._drawUnstyledText : this._drawUnstyledText;
     if (this._streaming && !isInitialContent) {
+      if (this._lastHighlights.length > 0) {
+        const chunks = treeSitterToTextChunks(content, this._lastHighlights, this._syntaxStyle, {
+          enabled: this._conceal
+        });
+        const styledText = new StyledText(chunks);
+        this.textBuffer.setStyledText(styledText);
+      } else {
+        this.textBuffer.setText(content);
+      }
+      this.updateTextInfo();
       this._shouldRenderTextBuffer = true;
     } else if (shouldDrawUnstyledNow) {
       this.textBuffer.setText(content);
